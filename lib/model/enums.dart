enum Operator {
  plus,
  minus,
  multiply,
  divided,
}

enum FilterType {
  drop,
  keep,
}

enum FilterCondition {
  none,
  lowest,
  highest,
}

enum Reroll {
  reroll,
  explode,
}

enum RerollCondition {
  only,
  more,
  less,
}
