enum Operator {
  plus,
  minus,
  multiply,
  divided,
}

enum FilterType {
  drop,
  keep,
}

enum FilterCondition {
  none,
  lowest,
  highest,
}

enum RerollType {
  reroll,
  explode,
}

enum RerollTimes {
  none,
  specific,
  always,
}

enum RerollCondition {
  none,
  only,
  more,
  less,
}
